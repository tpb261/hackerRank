import ast
T = int(input())
for t in range(0, T):
    M_dim = list(ast.literal_eval(','.join(input().split()))))
    a=[]
    for n in range(0, M_dim[0]):
        b = list(str(int(input())))
        M.extend(b[:M_dim[1]])
    m_dim = list(ast.literal_eval(','.join(input().split()))))
    for n in range(0, M_dim[0]):
        b = list(str(int(input())))
        m.extend(b[:m_dim[1]])
    
        
